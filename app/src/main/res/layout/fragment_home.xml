<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".FirstFragment">

    <EditText
        android:id="@+id/edittext_home"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintWidth_percent="0.80"
        android:background="@color/light_blue"
        android:layout_width="0dp"
        android:layout_height="40dp"
        android:paddingStart="20dp"
        android:paddingEnd="10dp"
        android:hint="@string/search_hint"
        />

    <!-- LinearLayout for tags -->
    <LinearLayout
        android:id="@+id/viewgroup_tags"
        app:layout_constraintTop_toBottomOf="@id/edittext_home"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintWidth_percent="0.80"

        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:paddingStart="20dp"
        android:paddingEnd="10dp"
        android:hint="@string/search_hint"
        >
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="White Rice"
            android:layout_marginEnd="10dp"
            android:padding="5dp"
            android:background="@color/pink">
        </TextView>
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Black Beans"
            android:layout_marginEnd="10dp"
            android:padding="5dp"
            android:background="@color/pink">
        </TextView>
    </LinearLayout>

    <!-- ListView for results -->
    <ListView
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintWidth_percent="0.80"
        app:layout_constraintTop_toBottomOf="@id/viewgroup_tags"
        app:layout_constraintBottom_toTopOf="@id/player_home"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:background="@color/light_green">
    </ListView>


    <!-- Player -->
    <LinearLayout
        android:id="@+id/player_home"
        android:layout_width="0dp"
        android:layout_height="50dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintWidth_percent="0.80"
        android:background="@color/light_blue">
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>